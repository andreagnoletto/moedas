[project]
name = "moedas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.2",
    "django-environ>=0.13.0",
    "gunicorn>=25.2.0",
    "openai>=2.21.0",
    "psycopg[binary]>=3.3.3",
]
